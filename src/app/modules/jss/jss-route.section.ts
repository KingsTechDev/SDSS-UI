import {Provider, Type} from '@angular/core';
import {Routes} from '@angular/router';

export const JSS_COMPONENTS: Array<Type<any>> = [
];

export const JSS_SERVICES: Array<Provider> = [
];

export const JSS_ROUTES: Routes = [
  {}
];
