import {Provider, Type} from '@angular/core';
import {Routes} from '@angular/router';

export const MTG_COMPONENTS: Array<Type<any>> = [
];

export const MGT_SERVICES: Array<Provider> = [
];

export const MGT_ROUTES: Routes = [
  {}
];
