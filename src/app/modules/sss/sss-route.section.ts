import {Provider, Type} from '@angular/core';
import {Routes} from '@angular/router';


export const SSS_COMPONENTS: Array<Type<any>> = [
];

export const SSS_SERVICES: Array<Provider> = [
];

export const SSS_ROUTES: Routes = [
  {}
];
